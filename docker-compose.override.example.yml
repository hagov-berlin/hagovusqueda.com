# docker-compose.override.yml
version: "3.9"
services:
  api:
    command: npm run dev
    volumes:
      - ./api:/app
      - /app/node_modules

  frontend:
    command: npm run dev
    volumes:
      - ./frontend:/app
      - /app/node_modules
